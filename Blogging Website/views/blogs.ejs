<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogs</title>
    <style>
        body {
            width: 100%;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
            color: #333;
        }
        .container {
            padding: 20px;
            display: flex;
            flex-direction: column;
            gap: 20px;
            max-width: 800px;
            margin: 0 auto;
            background: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }
        .heading {
            font-size: 28px;
            color: #2c3e50;
            border-bottom: 2px solid #3498db;
            padding-bottom: 10px;
        }
        p {
            font-size: 18px;
            line-height: 1.6;
            color: #7f8c8d;
        }
        h2 {
            font-size: 22px;
            color: #e74c3c;
            text-align: center;
        }
        @media (max-width: 550px) {
            .heading {
                font-size: 24px;
            }
            .container {
                padding: 15px;
            }
            p {
                font-size: 16px;
            }
        }
    </style>
</head>

<body>

    <%- include("header") %>
        <div class="container">
            <% if (array.length > 0) { %>
                <% array.forEach(function(object) { %>
                    <h1 class="heading">
                        <%= object.title %>
                    </h1>
                    <p>
                        <%= object.content %>
                    </p>
                <% }) %>
            <% } else { %>
                <h2>Sorry, No Blog Has Been Written Yet.</h2>
            <% } %>
        </div>
    <%- include("footer") %>

</body>

</html>
